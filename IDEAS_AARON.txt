# Busca la palabra clave en el archivo
if grep -q "$palabra_clave" "$archivo"; then
    echo "La palabra clave '$palabra_clave' se encuentra en el archivo."
else
    echo "La palabra clave '$palabra_clave' NO se encuentra en el archivo."
fi
