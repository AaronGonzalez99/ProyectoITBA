#!/bin/bash

# Define la nueva versión
nueva_version="2.0"  # Asigna el valor de la versión aquí

# Archivo YAML a modificar
archivo_yaml="ruta_al_archivo.yml"

# Modifica el valor de "version" en el archivo YAML
sed -i "s/^version: .*/version: ${nueva_version}/" "$archivo_yaml"
